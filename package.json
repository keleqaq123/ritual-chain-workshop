{
  "name": "ritual-private-bounty-judge",
  "version": "1.0.0",
  "private": true,
  "description": "Commit-reveal AI Bounty Judge with a Ritual-native private-judging architecture note.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js",
    "deploy:ritual": "hardhat run scripts/deploy.js --network ritual",
    "commitment": "node scripts/generate-commitment.js",
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.10"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
